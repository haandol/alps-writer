[project]
name = "alps-mcp-server"
version = "0.1.0"
description = "ALPS Writer MCP Server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp",
]

[project.scripts]
alps-mcp-server = "alps_mcp_server:main"

[tool.uv]
package = true
